if [[ -f bootstrap/config/datacenter.env ]]; then
  source_env bootstrap/config/datacenter.env
  export KUBECONFIG="$KUBECONFIG_PATH"
else
  log_status "bootstrap/config/datacenter.env missing; KUBECONFIG not set"
fi
